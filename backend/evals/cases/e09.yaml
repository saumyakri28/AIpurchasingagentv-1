id: E9
description: Real demand spike — increase qty (do not keep the stale 180 rec).
world: demand_change
intake:
  scenario: demand-change
  variant: increase
  payload:
    sku: SKU-SPIKE
    node: DC-NORTH
expected:
  decision_class: modify
  quantity_range: [216, 360]
  must_call_tools:
    - get_demand_stats
    - get_forecast
    - compute_replenishment_plan
    - validate_action
  must_not_violate: [C4, C7]
  expected_action:
    type: create_purchase_order
    target: SKU-SPIKE
    status: confirmed
  must_post_verify: true
  must_escalate_if_blocked: true
  recovery: false
llm: fake
